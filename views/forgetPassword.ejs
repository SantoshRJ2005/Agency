<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password | SharingYatra</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* --- Global Styles --- */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #ffffff;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* --- Main Layout Wrapper --- */
        .page-wrapper {
            display: flex;
            min-height: 100vh;
        }

        /* --- 1. Left Brand Panel --- */
        .brand-panel {
            flex-basis: 40%;
            background-color: #37475a;
            color: #ffffff;
            padding: 4rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .brand-panel .logo {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }
        .logo-sharing { color: #ffffff; }
        .logo-yatra { color: #f0ad4e; opacity: 0.9; }

        .brand-panel h2 {
            font-size: 2.25rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }
        .brand-panel p {
            font-size: 1rem;
            line-height: 1.6;
            color: #f0f0f0;
            max-width: 400px;
        }

        /* --- 2. Right Form Panel --- */
        .form-panel {
            flex-basis: 60%;
            background-color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            box-sizing: border-box;
        }

        .form-container {
            width: 100%;
            max-width: 450px;
        }

        .form-container h2 {
            font-size: 2rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
        }

        .form-container .subtitle {
            font-size: 1rem;
            color: #777;
            margin-bottom: 2rem;
        }

        /* --- EJS Message Styling (Handles Errors and Success) --- */
        .message-box {
            font-size: 0.9rem;
            font-weight: 500;
            margin-bottom: 1.5rem;
            padding: 0.75rem 1rem;
            border-radius: 6px;
            border: 1px solid transparent;
        }
        .message-box.error {
            color: #D9534F; /* Red */
            background-color: #fde8e8;
            border-color: #f5c6cb;
        }
        .message-box:empty {
            display: none;
        }
        
        /* --- Form Styling (Matching Signup Page) --- */
        .form-field-group {
            margin-bottom: 1.5rem;
            text-align: left;
        }

        .form-field-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            font-size: 0.9rem;
            color: #555;
        }

        /* Shared style for input and select */
        .input-wrapper {
            display: flex;
            align-items: center;
            background-color: #f7f8fa;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }
        
        .input-wrapper.select-wrapper {
            padding-right: 0.75rem; /* Space for dropdown arrow */
        }

        .input-wrapper svg {
            width: 20px;
            height: 20px;
            fill: #888;
            margin: 0 0.75rem;
            transition: fill 0.2s ease;
            flex-shrink: 0; /* Prevent icon from shrinking */
        }

        .input-wrapper input,
        .input-wrapper select {
            flex: 1;
            border: none;
            outline: none;
            background: transparent;
            padding: 0.85rem 0;
            font-size: 1rem;
            font-family: 'Poppins', sans-serif;
            color: #333;
            width: 100%;
        }

        /* Remove default select styling */
        .input-wrapper select {
            appearance: none;
            -webkit-appearance: none;
            -moz-appearance: none;
        }

        .input-wrapper:focus-within {
            border-color: #FF6B6B;
            box-shadow: 0 0 0 3px rgba(255, 107, 107, 0.2);
        }
        .input-wrapper:focus-within svg {
            fill: #FF6B6B;
        }

        .action-btn {
            width: 100%;
            padding: 0.85rem;
            border: none;
            border-radius: 6px;
            background-color: #FF6B6B;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            margin-top: 1rem;
        }
        .action-btn:hover {
            background-color: #e65a5a;
        }

        .form-link {
            text-align: center;
            margin-top: 1.5rem;
            color: #555;
            font-size: 0.9rem;
        }
        .form-link a {
            color: #FF6B6B; 
            text-decoration: none;
            font-weight: 600;
        }
        .form-link a:hover {
            text-decoration: underline;
        }
        
        @media (max-width: 900px) {
            .page-wrapper { flex-direction: column; }
            .brand-panel { flex-basis: auto; padding: 3rem 2rem; text-align: center; }
            .brand-panel p { max-width: 100%; }
            .form-panel { flex-basis: auto; }
        }
    </style>
</head>
<body>
    <div class="page-wrapper">
        <div class="brand-panel">
            <h1 class="logo">
                <span class="logo-sharing">Sharing</span><span class="logo-yatra">Yatra</span>
            </h1>
            <h2>Account Recovery</h2>
            <p>
                Please verify your account details to reset your password. 
                This ensures your account remains secure.
            </p>
        </div>

        <div class="form-panel">
            <div class="form-container">
                <h2>Forgot Your Password?</h2>
                <p class="subtitle">Enter your details to verify your account.</p>

                <!-- EJS Message Box -->
                <div class="message-box <% if (typeof isError !== 'undefined' && isError) { %>error<% } %>">
                    <% if (typeof message !== 'undefined' && message) { %>
                        <%= message %>
                    <% } %>
                </div>

                <form action="/forgetPassword" method="POST">
                    
                    <!-- Email Field -->
                    <div class="form-field-group">
                        <label for="email">Agency Email ID</label>
                        <div class="input-wrapper">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>
                            <input type="email" id="email" name="email" placeholder="you@agency.com" required>
                        </div>
                    </div>
                    
                    <!-- Mobile Number Field -->
                    <div class="form-field-group">
                        <label for="agencyMobile">Agency Mobile Number</label>
                        <div class="input-wrapper">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M17 1.01L7 1c-1.1 0-2 .9-2 2v18c0 1.1.9 2 2 2h10c1.1 0 2-.9 2-2V3c0-1.1-.9-1.99-2-1.99zM17 19H7V5h10v14z"/></svg>
                            <input type="text" id="agencyMobile" name="agencyMobile" placeholder="e.g. 9876543210" required>
                        </div>
                    </div>

                    <!-- Document Type Select -->
                    <div class="form-field-group">
                        <label for="documentType">Verification Document</label>
                        <div class="input-wrapper select-wrapper">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M8.5 13.5l2.5 3 3.5-4.5 4.5 6H5m16 1V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2z"/></svg>
                            <select id="documentType" name="documentType" required>
                                <option value="" disabled selected>Select a document type</option>
                                <option value="gstNumber">GST Number</option>
                                <option value="panNumber">PAN Number</option>
                                <option value="agencyLicense">Agency License Number</option>
                            </select>
                        </div>
                    </div>
                    
                    <!-- Document Value Input -->
                    <div class="form-field-group">
                        <label for="documentValue">Document Number</label>
                        <div class="input-wrapper">
                             <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M14 2H6c-1.1 0-1.99.9-1.99 2L4 20c0 1.1.89 2 1.99 2H18c1.1 0 2-.9 2-2V8l-6-6zM6 20V4h7v5h5v11H6z"/></svg>
                            <input type.text" id="documentValue" name="documentValue" placeholder="Enter the document number" required>
                        </div>
                    </div>
                    
                    <button type="submit" class="action-btn">Verify Account</button>
                </form>

                <p class="form-link">
                    Remembered your password? <a href="/login">Back to Login</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>