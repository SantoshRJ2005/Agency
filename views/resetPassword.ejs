<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password | SharingYatra</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* Styles are identical to forgetPassword.ejs for consistency */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #ffffff;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .page-wrapper { display: flex; min-height: 100vh; }
        .brand-panel {
            flex-basis: 40%;
            background-color: #37475a;
            color: #ffffff;
            padding: 4rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        .brand-panel .logo { font-size: 2.5rem; font-weight: 700; margin-bottom: 1rem; }
        .logo-sharing { color: #ffffff; }
        .logo-yatra { color: #f0ad4e; opacity: 0.9; }
        .brand-panel h2 { font-size: 2.25rem; font-weight: 600; margin-bottom: 1rem; }
        .brand-panel p { font-size: 1rem; line-height: 1.6; color: #f0f0f0; max-width: 400px; }
        .form-panel {
            flex-basis: 60%;
            background-color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            box-sizing: border-box;
        }
        .form-container { width: 100%; max-width: 450px; }
        .form-container h2 { font-size: 2rem; font-weight: 600; color: #333; margin-bottom: 0.5rem; }
        .form-container .subtitle { font-size: 1rem; color: #777; margin-bottom: 2rem; }
        .message-box {
            font-size: 0.9rem;
            font-weight: 500;
            margin-bottom: 1.5rem;
            padding: 0.75rem 1rem;
            border-radius: 6px;
            border: 1px solid transparent;
        }
        .message-box.error {
            color: #D9534F;
            background-color: #fde8e8;
            border-color: #f5c6cb;
        }
        .message-box:empty { display: none; }
        .form-field-group { margin-bottom: 1.5rem; text-align: left; }
        .form-field-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            font-size: 0.9rem;
            color: #555;
        }
        .input-wrapper {
            display: flex;
            align-items: center;
            background-color: #f7f8fa;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 0 0.75rem;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }
        .input-wrapper svg {
            width: 20px;
            height: 20px;
            fill: #888;
            margin-right: 0.75rem;
            transition: fill 0.2s ease;
        }
        .input-wrapper input {
            flex: 1;
            border: none;
            outline: none;
            background: transparent;
            padding: 0.85rem 0;
            font-size: 1rem;
            font-family: 'Poppins', sans-serif;
            color: #333;
        }
        .input-wrapper:focus-within {
            border-color: #FF6B6B;
            box-shadow: 0 0 0 3px rgba(255, 107, 107, 0.2);
        }
        .input-wrapper:focus-within svg { fill: #FF6B6B; }
        .action-btn {
            width: 100%;
            padding: 0.85rem;
            border: none;
            border-radius: 6px;
            background-color: #FF6B6B;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            margin-top: 1rem;
        }
        .action-btn:hover { background-color: #e65a5a; }
        .form-link {
            text-align: center;
            margin-top: 1.5rem;
            color: #555;
            font-size: 0.9rem;
        }
        .form-link a { color: #FF6B6B; text-decoration: none; font-weight: 600; }
        .form-link a:hover { text-decoration: underline; }

        @media (max-width: 900px) {
            .page-wrapper { flex-direction: column; }
            .brand-panel { flex-basis: auto; padding: 3rem 2rem; text-align: center; }
            .brand-panel p { max-width: 100%; }
            .form-panel { flex-basis: auto; }
        }
    </style>
</head>
<body>
    <div class="page-wrapper">
        <div class="brand-panel">
            <h1 class="logo">
                <span class="logo-sharing">Sharing</span><span class="logo-yatra">Yatra</span>
            </h1>
            <h2>Account Security</h2>
            <p>
                Create a new, strong password for your account. 
                Do not share this password with anyone.
            </p>
        </div>

        <div class="form-panel">
            <div class="form-container">
                <h2>Set New Password</h2>
                <p class="subtitle">You are verified. Please enter a new password.</p>

                <!-- EJS Message Box -->
                <div class="message-box <% if (typeof isError !== 'undefined' && isError) { %>error<% } %>">
                    <% if (typeof message !== 'undefined' && message) { %>
                        <%= message %>
                    <% } %>
                </div>

                <form action="/resetPassword" method="POST">
                    
                    <!-- New Password Field -->
                    <div class="form-field-group">
                        <label for="password">New Password</label>
                        <div class="input-wrapper">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zm-6 9c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2zm3.1-9H8.9V6c0-1.71 1.39-3.1 3.1-3.1 1.71 0 3.1 1.39 3.1 3.1v2z"/></svg>
                            <input type="password" id="password" name="password" placeholder="••••••••" required>
                        </div>
                    </div>
                    
                    <!-- Confirm Password Field -->
                    <div class="form-field-group">
                        <label for="confirmPassword">Confirm New Password</label>
                        <div class="input-wrapper">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zm-6 9c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2zm3.1-9H8.9V6c0-1.71 1.39-3.1 3.1-3.1 1.71 0 3.1 1.39 3.1 3.1v2z"/></svg>
                            <input type="password" id="confirmPassword" name="confirmPassword" placeholder="••••••••" required>
                        </div>
                    </div>
                    
                    <button type="submit" class="action-btn">Save New Password</button>
                </form>

                <p class="form-link">
                    <a href="/login">Back to Login</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>